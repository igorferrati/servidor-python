apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wscgi.fullname" . }}-test-connection"
  labels:
    {{- include "wscgi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wscgi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
