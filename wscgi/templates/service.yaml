apiVersion: v1
kind: Service
metadata:
  name: {{ include "wscgi.fullname" . }}
  labels:
    {{- include "wscgi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wscgi.selectorLabels" . | nindent 4 }}
